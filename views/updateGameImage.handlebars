{{> partials/imageUploadForm
    formId="game-image-form"
    inputId="game-upload"
    inputName="gameImage"
    labelText="Upload new event image"
    hiddenIdElementId="game-id"
    hiddenIdValue=gameObj._id}}
